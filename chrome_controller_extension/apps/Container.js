export class Container
{
    constructor(selectors, itemsSelectors, direction)
    {
        this.selectors = selectors;
        this.itemsSelectors = itemsSelectors;
        this.direction = direction;
    }
}